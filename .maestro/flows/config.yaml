# CI runs login + create-todo + demo smokes. signup.yaml is kept for local runs
# (new users must complete consent modals, which this smoke set does not cover).
flows:
  - "login.yaml"
  - "create-todo.yaml"
  - "demo/*"
