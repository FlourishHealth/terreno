/** Debounce delay for admin list search input before firing list queries. */
export const ADMIN_SEARCH_DEBOUNCE_MS = 250;

/** Viewport width below which filter UI uses a bottom sheet instead of a side drawer. */
export const ADMIN_FILTER_MOBILE_BREAKPOINT = 768;
