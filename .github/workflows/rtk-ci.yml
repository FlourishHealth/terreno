name: RTK CI

on:
  push:
    paths:
      - "rtk/**"
      - ".github/workflows/rtk-ci.yml"

jobs:
  lint-and-build:
    name: RTK Lint and Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Cache Bun dependencies
        id: cache
        uses: actions/cache@v5
        with:
          path: |
            ~/.bun/install/cache
            node_modules
          key: bun-${{ runner.os }}-${{ github.ref }}-${{ hashFiles('bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ github.ref }}-
            bun-${{ runner.os }}-

      - name: Debug cache status
        run: |
          echo "=== Cache Debug Info ==="
          echo "Cache hit: ${{ steps.cache.outputs.cache-hit }}"
          echo "Cache key: bun-${{ runner.os }}-${{ github.ref }}-${{ hashFiles('bun.lock') }}"
          echo ""
          echo "=== Bun cache directory ==="
          if [ -d ~/.bun/install/cache ]; then
            echo "Bun cache exists"
            du -sh ~/.bun/install/cache || echo "Could not get size"
            ls -la ~/.bun/install/cache | head -20 || echo "Could not list"
          else
            echo "Bun cache does NOT exist"
          fi
          echo ""
          echo "=== node_modules directory ==="
          if [ -d node_modules ]; then
            echo "node_modules exists"
            du -sh node_modules || echo "Could not get size"
            ls -la node_modules | head -20 || echo "Could not list"
          else
            echo "node_modules does NOT exist"
          fi

      - name: Install dependencies
        run: |
          echo "Starting bun install at $(date)"
          time bun install --frozen-lockfile
          echo "Finished bun install at $(date)"

      - name: Debug post-install
        run: |
          echo "=== Post-install Debug ==="
          echo "node_modules size:"
          du -sh node_modules || echo "Could not get size"
          echo "Package count:"
          ls -1 node_modules | wc -l || echo "Could not count"

      # Build UI first since RTK depends on it
      - name: Build UI dependency
        run: bun run compile
        working-directory: ui

      - name: Lint
        run: bun run lint
        working-directory: rtk

      - name: Build
        run: bun run compile
        working-directory: rtk

