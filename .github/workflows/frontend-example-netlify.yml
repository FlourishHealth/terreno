name: Frontend Example Netlify Deploy

on:
  push:
    branches:
      - master
    paths:
      - "frontend-example/**"
      - "ui/**"
      - "rtk/**"
      - ".github/workflows/frontend-example-netlify.yml"
  pull_request:
    paths:
      - "frontend-example/**"
      - "ui/**"
      - "rtk/**"
      - ".github/workflows/frontend-example-netlify.yml"

jobs:
  build-and-deploy:
    name: Build and Deploy Frontend Example
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install root dependencies (workspace)
        run: bun install --frozen-lockfile

      - name: Build UI dependency
        run: bun run compile
        working-directory: ui

      - name: Build RTK dependency
        run: bun run compile
        working-directory: rtk

      - name: Debug - List directories
        run: ls -la

      - name: Export frontend-example for web
        run: |
          cd frontend-example
          bun run export

      - name: Deploy to Netlify
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: "./frontend-example/dist"
          production-branch: master
          production-deploy: ${{ github.ref == 'refs/heads/master' }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          deploy-message: "Deploy from GitHub Actions - ${{ github.event.head_commit.message || github.event.pull_request.title }}"
          enable-pull-request-comment: true
          enable-commit-comment: true
          overwrites-pull-request-comment: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_FRONTEND_EXAMPLE_SITE_ID }}
        timeout-minutes: 5
