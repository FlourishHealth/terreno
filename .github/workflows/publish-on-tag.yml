name: Publish packages on tag

# Disabled after migration to CircleCI semver-tag publishing; retained for rollback.
on:
  push:
    branches-ignore:
      - "**"

jobs:
  breaking-change-documentation:
    name: Breaking changes have upgrade notes
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Check release upgrade documentation
        run: bun run check:upgrade-docs "${{ github.ref_name }}"

  check-changes:
    needs: breaking-change-documentation
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      npmTag: ${{ steps.version.outputs.npmTag }}
      isPrerelease: ${{ steps.version.outputs.isPrerelease }}
    steps:
      - uses: actions/checkout@v6.0.3

      - name: Get version from tag
        id: version
        run: |
          # Extract version from tag (e.g., 1.0.0)
          VERSION=${GITHUB_REF#refs/tags/}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

          # npm publishes to the `latest` dist-tag unless told otherwise, so a
          # prerelease must name its own channel or it becomes the default install
          # for everyone. The channel is the prerelease identifier itself
          # (1.2.3-beta.0 -> beta, 1.2.3-rc.1 -> rc).
          case "$VERSION" in
            *-*)
              PRERELEASE_ID=${VERSION#*-}
              NPM_TAG=${PRERELEASE_ID%%.*}
              echo "isPrerelease=true" >> $GITHUB_OUTPUT
              ;;
            *)
              NPM_TAG=latest
              echo "isPrerelease=false" >> $GITHUB_OUTPUT
              ;;
          esac
          echo "npmTag=$NPM_TAG" >> $GITHUB_OUTPUT
          echo "npm dist-tag: $NPM_TAG"

  publish-api:
    needs: check-changes
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: api
    steps:
      - uses: actions/checkout@v6.0.3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Use Node.js 22
        uses: actions/setup-node@v7
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Validate required secrets
        run: |
          missing=()
          if [ -z "${{ secrets.NPM_TOKEN }}" ]; then missing+=("NPM_TOKEN"); fi
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required secrets: ${missing[*]}"
            exit 1
          fi

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ github.ref }}-${{ hashFiles('bun.lock', 'package.json') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ github.ref }}-
            bun-${{ runner.os }}-

      - name: Start MongoDB
        uses: supercharge/mongodb-github-action@1.12.1
        with:
          mongodb-version: 6.0
          mongodb-image: mirror.gcr.io/library/mongo

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Update version
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); pkg.version = '$VERSION'; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');"

      - name: Replace catalog and workspace references
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "
          const fs = require('fs');
          const rootPkg = JSON.parse(fs.readFileSync('../package.json', 'utf8'));
          const catalog = rootPkg.catalog || {};
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          const version = process.env.VERSION;
          for (const depType of ['dependencies', 'devDependencies', 'peerDependencies']) {
            if (!pkg[depType]) continue;
            for (const [key, value] of Object.entries(pkg[depType])) {
              if (value === 'catalog:') {
                if (!catalog[key]) throw new Error(\`Catalog entry not found for \${key}\`);
                pkg[depType][key] = catalog[key];
              } else if (value === 'workspace:*' && key.startsWith('@terreno/')) {
                pkg[depType][key] = version;
              }
            }
          }
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "
        env:
          VERSION: ${{ needs.check-changes.outputs.version }}

      - name: Compile
        run: bun run compile

      - name: Run tests
        run: bun run test

      - name: Publish to npm
        run: npm publish --tag "${{ needs.check-changes.outputs.npmTag }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: echo "Published @terreno/api@${{ needs.check-changes.outputs.version }}" >> $GITHUB_STEP_SUMMARY

  publish-test:
    needs: check-changes
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: test
    steps:
      - uses: actions/checkout@v6.0.3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Use Node.js 22
        uses: actions/setup-node@v7
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Validate required secrets
        run: |
          missing=()
          if [ -z "${{ secrets.NPM_TOKEN }}" ]; then missing+=("NPM_TOKEN"); fi
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required secrets: ${missing[*]}"
            exit 1
          fi

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ github.ref }}-${{ hashFiles('bun.lock', 'package.json') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ github.ref }}-
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Update version
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); pkg.version = '$VERSION'; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');"

      - name: Replace catalog references
        run: |
          node -e "
          const fs = require('fs');
          const rootPkg = JSON.parse(fs.readFileSync('../package.json', 'utf8'));
          const catalog = rootPkg.catalog || {};
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          for (const depType of ['dependencies', 'devDependencies', 'peerDependencies']) {
            if (!pkg[depType]) continue;
            for (const [key, value] of Object.entries(pkg[depType])) {
              if (value === 'catalog:') {
                if (!catalog[key]) throw new Error(\`Catalog entry not found for \${key}\`);
                pkg[depType][key] = catalog[key];
              }
            }
          }
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "

      - name: Compile
        run: bun run compile

      - name: Publish to npm
        run: npm publish --tag "${{ needs.check-changes.outputs.npmTag }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: echo "Published @terreno/test@${{ needs.check-changes.outputs.version }}" >> $GITHUB_STEP_SUMMARY

  publish-ui:
    needs: check-changes
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: ui
    steps:
      - uses: actions/checkout@v6.0.3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Use Node.js 22
        uses: actions/setup-node@v7
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Validate required secrets
        run: |
          missing=()
          if [ -z "${{ secrets.NPM_TOKEN }}" ]; then missing+=("NPM_TOKEN"); fi
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required secrets: ${missing[*]}"
            exit 1
          fi

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ github.ref }}-${{ hashFiles('bun.lock', 'package.json') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ github.ref }}-
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Update version
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); pkg.version = '$VERSION'; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');"

      - name: Get latest @terreno/ui version
        id: ui_version
        run: |
          # `npm view @terreno/ui version` resolves the `latest` dist-tag, which for a
          # prerelease is the previous stable line. Pinning to it would ship a beta that
          # depends on stable UI. Every consumer of this step needs publish-ui, so the
          # matching prerelease UI is already on npm by the time this runs.
          if [ "${{ needs.check-changes.outputs.isPrerelease }}" == "true" ]; then
            UI_VERSION="${{ needs.check-changes.outputs.version }}"
            echo "Prerelease: pinning @terreno/ui to this release's version"
          else
            UI_VERSION=$(npm view @terreno/ui version 2>/dev/null || echo "")
          fi
          if [ -z "$UI_VERSION" ]; then
            echo "Warning: Could not fetch @terreno/ui version from npm, using current release version"
            UI_VERSION="${{ needs.check-changes.outputs.version }}"
          fi
          echo "ui_version=$UI_VERSION" >> $GITHUB_OUTPUT
          echo "Using @terreno/ui version: $UI_VERSION"

      - name: Replace catalog references
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          UI_VERSION="${{ steps.ui_version.outputs.ui_version }}"
          node -e "
          const fs = require('fs');
          const path = require('path');
          
          // Read root catalog
          const rootPkg = JSON.parse(fs.readFileSync('../package.json', 'utf8'));
          const catalog = rootPkg.catalog || {};
          
          // Read current package.json
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          const version = process.env.VERSION;
          const uiVersion = process.env.UI_VERSION;
          
          // Replace catalog: in dependencies
          if (pkg.dependencies) {
            for (const [key, value] of Object.entries(pkg.dependencies)) {
              if (value === 'catalog:') {
                if (catalog[key]) {
                  pkg.dependencies[key] = catalog[key];
                } else {
                  throw new Error(\`Catalog entry not found for \${key}\`);
                }
              } else if (value === 'workspace:*' && key === '@terreno/ui') {
                // Use latest published version of @terreno/ui
                pkg.dependencies[key] = uiVersion;
              } else if (value === 'workspace:*' && key.startsWith('@terreno/')) {
                // Replace workspace:* with current release version for other @terreno packages
                pkg.dependencies[key] = version;
              }
            }
          }
          
          // Replace catalog: in devDependencies
          if (pkg.devDependencies) {
            for (const [key, value] of Object.entries(pkg.devDependencies)) {
              if (value === 'catalog:') {
                if (catalog[key]) {
                  pkg.devDependencies[key] = catalog[key];
                } else {
                  throw new Error(\`Catalog entry not found for \${key}\`);
                }
              } else if (value === 'workspace:*' && key === '@terreno/ui') {
                // Use latest published version of @terreno/ui
                pkg.devDependencies[key] = uiVersion;
              } else if (value === 'workspace:*' && key.startsWith('@terreno/')) {
                // Replace workspace:* with current release version for other @terreno packages
                pkg.devDependencies[key] = version;
              }
            }
          }
          
          // Replace catalog: in peerDependencies
          if (pkg.peerDependencies) {
            for (const [key, value] of Object.entries(pkg.peerDependencies)) {
              if (value === 'catalog:') {
                if (catalog[key]) {
                  pkg.peerDependencies[key] = catalog[key];
                } else {
                  throw new Error(\`Catalog entry not found for \${key}\`);
                }
              } else if (value === 'workspace:*' && key === '@terreno/ui') {
                // Use latest published version of @terreno/ui
                pkg.peerDependencies[key] = uiVersion;
              } else if (value === 'workspace:*' && key.startsWith('@terreno/')) {
                // Replace workspace:* with current release version for other @terreno packages
                pkg.peerDependencies[key] = version;
              }
            }
          }
          
          // Write updated package.json
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "
        env:
          VERSION: ${{ needs.check-changes.outputs.version }}
          UI_VERSION: ${{ steps.ui_version.outputs.ui_version }}

      - name: Compile
        run: bun run compile

      - name: Run tests
        run: bun run test:ci

      - name: Publish to npm
        run: npm publish --tag "${{ needs.check-changes.outputs.npmTag }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: echo "Published @terreno/ui@${{ needs.check-changes.outputs.version }}" >> $GITHUB_STEP_SUMMARY

  publish-rtk:
    needs: [check-changes, publish-ui]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: rtk
    steps:
      - uses: actions/checkout@v6.0.3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Use Node.js 22
        uses: actions/setup-node@v7
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Validate required secrets
        run: |
          missing=()
          if [ -z "${{ secrets.NPM_TOKEN }}" ]; then missing+=("NPM_TOKEN"); fi
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required secrets: ${missing[*]}"
            exit 1
          fi

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ github.ref }}-${{ hashFiles('bun.lock', 'package.json') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ github.ref }}-
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Update version
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); pkg.version = '$VERSION'; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');"

      - name: Get latest @terreno/ui version
        id: ui_version
        run: |
          # `npm view @terreno/ui version` resolves the `latest` dist-tag, which for a
          # prerelease is the previous stable line. Pinning to it would ship a beta that
          # depends on stable UI. Every consumer of this step needs publish-ui, so the
          # matching prerelease UI is already on npm by the time this runs.
          if [ "${{ needs.check-changes.outputs.isPrerelease }}" == "true" ]; then
            UI_VERSION="${{ needs.check-changes.outputs.version }}"
            echo "Prerelease: pinning @terreno/ui to this release's version"
          else
            UI_VERSION=$(npm view @terreno/ui version 2>/dev/null || echo "")
          fi
          if [ -z "$UI_VERSION" ]; then
            echo "Warning: Could not fetch @terreno/ui version from npm, using current release version"
            UI_VERSION="${{ needs.check-changes.outputs.version }}"
          fi
          echo "ui_version=$UI_VERSION" >> $GITHUB_OUTPUT
          echo "Using @terreno/ui version: $UI_VERSION"

      - name: Replace catalog references
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          UI_VERSION="${{ steps.ui_version.outputs.ui_version }}"
          node -e "
          const fs = require('fs');
          const path = require('path');

          // Read root catalog
          const rootPkg = JSON.parse(fs.readFileSync('../package.json', 'utf8'));
          const catalog = rootPkg.catalog || {};

          // Read current package.json
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          const version = process.env.VERSION;
          const uiVersion = process.env.UI_VERSION;

          // Replace catalog: in dependencies
          if (pkg.dependencies) {
            for (const [key, value] of Object.entries(pkg.dependencies)) {
              if (value === 'catalog:') {
                if (catalog[key]) {
                  pkg.dependencies[key] = catalog[key];
                } else {
                  throw new Error(\`Catalog entry not found for \${key}\`);
                }
              } else if (value === 'workspace:*' && key === '@terreno/ui') {
                // Use latest published version of @terreno/ui
                pkg.dependencies[key] = uiVersion;
              } else if (value === 'workspace:*' && key.startsWith('@terreno/')) {
                // Replace workspace:* with current release version for other @terreno packages
                pkg.dependencies[key] = version;
              }
            }
          }

          // Replace catalog: in devDependencies
          if (pkg.devDependencies) {
            for (const [key, value] of Object.entries(pkg.devDependencies)) {
              if (value === 'catalog:') {
                if (catalog[key]) {
                  pkg.devDependencies[key] = catalog[key];
                } else {
                  throw new Error(\`Catalog entry not found for \${key}\`);
                }
              } else if (value === 'workspace:*' && key === '@terreno/ui') {
                // Use latest published version of @terreno/ui
                pkg.devDependencies[key] = uiVersion;
              } else if (value === 'workspace:*' && key.startsWith('@terreno/')) {
                // Replace workspace:* with current release version for other @terreno packages
                pkg.devDependencies[key] = version;
              }
            }
          }

          // Replace catalog: in peerDependencies
          if (pkg.peerDependencies) {
            for (const [key, value] of Object.entries(pkg.peerDependencies)) {
              if (value === 'catalog:') {
                if (catalog[key]) {
                  pkg.peerDependencies[key] = catalog[key];
                } else {
                  throw new Error(\`Catalog entry not found for \${key}\`);
                }
              } else if (value === 'workspace:*' && key === '@terreno/ui') {
                // Use latest published version of @terreno/ui
                pkg.peerDependencies[key] = uiVersion;
              } else if (value === 'workspace:*' && key.startsWith('@terreno/')) {
                // Replace workspace:* with current release version for other @terreno packages
                pkg.peerDependencies[key] = version;
              }
            }
          }

          // Write updated package.json
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "
        env:
          VERSION: ${{ needs.check-changes.outputs.version }}
          UI_VERSION: ${{ steps.ui_version.outputs.ui_version }}

      - name: Install dependencies after replacement
        run: bun install

      - name: Compile workspace dependencies
        run: node ../.github/scripts/compile-workspace-deps.js

      - name: Compile
        run: bun run compile

      - name: Publish to npm
        run: npm publish --tag "${{ needs.check-changes.outputs.npmTag }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: echo "Published @terreno/rtk@${{ needs.check-changes.outputs.version }}" >> $GITHUB_STEP_SUMMARY

  publish-admin-backend:
    needs: [check-changes, publish-api, publish-test]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: admin-backend
    steps:
      - uses: actions/checkout@v6.0.3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Use Node.js 22
        uses: actions/setup-node@v7
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Validate required secrets
        run: |
          missing=()
          if [ -z "${{ secrets.NPM_TOKEN }}" ]; then missing+=("NPM_TOKEN"); fi
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required secrets: ${missing[*]}"
            exit 1
          fi

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ github.ref }}-${{ hashFiles('bun.lock', 'package.json') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ github.ref }}-
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Update version
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); pkg.version = '$VERSION'; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');"

      - name: Replace catalog and workspace references
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "
          const fs = require('fs');
          const rootPkg = JSON.parse(fs.readFileSync('../package.json', 'utf8'));
          const catalog = rootPkg.catalog || {};
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          const version = process.env.VERSION;
          for (const depType of ['dependencies', 'devDependencies', 'peerDependencies']) {
            if (!pkg[depType]) continue;
            for (const [key, value] of Object.entries(pkg[depType])) {
              if (value === 'catalog:') {
                if (!catalog[key]) throw new Error(\`Catalog entry not found for \${key}\`);
                pkg[depType][key] = catalog[key];
              } else if (value === 'workspace:*' && key.startsWith('@terreno/')) {
                pkg[depType][key] = version;
              }
            }
          }
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "
        env:
          VERSION: ${{ needs.check-changes.outputs.version }}

      - name: Install dependencies after replacement
        run: bun install

      - name: Compile workspace dependencies
        run: node ../.github/scripts/compile-workspace-deps.js

      - name: Compile
        run: bun run compile

      - name: Publish to npm
        run: npm publish --tag "${{ needs.check-changes.outputs.npmTag }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: echo "Published @terreno/admin-backend@${{ needs.check-changes.outputs.version }}" >> $GITHUB_STEP_SUMMARY

  publish-admin-frontend:
    needs: [check-changes, publish-ui]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: admin-frontend
    steps:
      - uses: actions/checkout@v6.0.3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Use Node.js 22
        uses: actions/setup-node@v7
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Validate required secrets
        run: |
          missing=()
          if [ -z "${{ secrets.NPM_TOKEN }}" ]; then missing+=("NPM_TOKEN"); fi
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required secrets: ${missing[*]}"
            exit 1
          fi

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ github.ref }}-${{ hashFiles('bun.lock', 'package.json') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ github.ref }}-
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Update version
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); pkg.version = '$VERSION'; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');"

      - name: Get latest @terreno/ui version
        id: ui_version
        run: |
          # `npm view @terreno/ui version` resolves the `latest` dist-tag, which for a
          # prerelease publish is still the prior stable release. Pin to the release
          # version when publishing a prerelease so admin packages depend on the matching UI.
          if [ "${{ needs.check-changes.outputs.isPrerelease }}" == "true" ]; then
            UI_VERSION="${{ needs.check-changes.outputs.version }}"
          else
            UI_VERSION=$(npm view @terreno/ui version 2>/dev/null || echo "")
          fi
          if [ -z "$UI_VERSION" ]; then
            UI_VERSION="${{ needs.check-changes.outputs.version }}"
          fi
          echo "ui_version=$UI_VERSION" >> $GITHUB_OUTPUT
          echo "Using @terreno/ui version: $UI_VERSION"

      - name: Replace catalog and workspace references
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          UI_VERSION="${{ steps.ui_version.outputs.ui_version }}"
          node -e "
          const fs = require('fs');
          const rootPkg = JSON.parse(fs.readFileSync('../package.json', 'utf8'));
          const catalog = rootPkg.catalog || {};
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          const version = process.env.VERSION;
          const uiVersion = process.env.UI_VERSION;
          for (const depType of ['dependencies', 'devDependencies', 'peerDependencies']) {
            if (!pkg[depType]) continue;
            for (const [key, value] of Object.entries(pkg[depType])) {
              if (value === 'catalog:') {
                if (!catalog[key]) throw new Error(\`Catalog entry not found for \${key}\`);
                pkg[depType][key] = catalog[key];
              } else if (value === 'workspace:*' && key === '@terreno/ui') {
                pkg[depType][key] = uiVersion;
              } else if (value === 'workspace:*' && key.startsWith('@terreno/')) {
                pkg[depType][key] = version;
              }
            }
          }
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "
        env:
          VERSION: ${{ needs.check-changes.outputs.version }}
          UI_VERSION: ${{ steps.ui_version.outputs.ui_version }}

      - name: Install dependencies after replacement
        run: bun install

      - name: Compile workspace dependencies
        run: node ../.github/scripts/compile-workspace-deps.js

      - name: Compile
        run: bun run compile

      - name: Publish to npm
        run: npm publish --tag "${{ needs.check-changes.outputs.npmTag }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: echo "Published @terreno/admin-frontend@${{ needs.check-changes.outputs.version }}" >> $GITHUB_STEP_SUMMARY

  publish-admin-spa:
    needs: [check-changes, publish-ui, publish-rtk, publish-admin-frontend]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: admin-spa
    steps:
      - uses: actions/checkout@v6.0.3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Use Node.js 22
        uses: actions/setup-node@v7
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Validate required secrets
        run: |
          missing=()
          if [ -z "${{ secrets.NPM_TOKEN }}" ]; then missing+=("NPM_TOKEN"); fi
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required secrets: ${missing[*]}"
            exit 1
          fi

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ github.ref }}-${{ hashFiles('bun.lock', 'package.json') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ github.ref }}-
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Update version
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); pkg.version = '$VERSION'; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');"

      # Build the static web export BEFORE replacing workspace deps, so metro resolves the
      # local workspace sources. The resulting dist/ is shipped in the published package.
      - name: Compile workspace dependencies
        run: node ../.github/scripts/compile-workspace-deps.js

      - name: Compile server plugin
        run: bun run compile

      - name: Build web export
        run: bun run build:web

      - name: Get latest @terreno/ui version
        id: ui_version
        run: |
          # `npm view @terreno/ui version` resolves the `latest` dist-tag, which for a
          # prerelease publish is still the prior stable release. Pin to the release
          # version when publishing a prerelease so admin packages depend on the matching UI.
          if [ "${{ needs.check-changes.outputs.isPrerelease }}" == "true" ]; then
            UI_VERSION="${{ needs.check-changes.outputs.version }}"
          else
            UI_VERSION=$(npm view @terreno/ui version 2>/dev/null || echo "")
          fi
          if [ -z "$UI_VERSION" ]; then
            UI_VERSION="${{ needs.check-changes.outputs.version }}"
          fi
          echo "ui_version=$UI_VERSION" >> $GITHUB_OUTPUT
          echo "Using @terreno/ui version: $UI_VERSION"

      - name: Replace catalog and workspace references
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          UI_VERSION="${{ steps.ui_version.outputs.ui_version }}"
          node -e "
          const fs = require('fs');
          const rootPkg = JSON.parse(fs.readFileSync('../package.json', 'utf8'));
          const catalog = rootPkg.catalog || {};
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          const version = process.env.VERSION;
          const uiVersion = process.env.UI_VERSION;
          for (const depType of ['dependencies', 'devDependencies', 'peerDependencies']) {
            if (!pkg[depType]) continue;
            for (const [key, value] of Object.entries(pkg[depType])) {
              if (value === 'catalog:') {
                if (!catalog[key]) throw new Error(\`Catalog entry not found for \${key}\`);
                pkg[depType][key] = catalog[key];
              } else if (value === 'workspace:*' && key === '@terreno/ui') {
                pkg[depType][key] = uiVersion;
              } else if (value === 'workspace:*' && key.startsWith('@terreno/')) {
                pkg[depType][key] = version;
              }
            }
          }
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "
        env:
          VERSION: ${{ needs.check-changes.outputs.version }}
          UI_VERSION: ${{ steps.ui_version.outputs.ui_version }}

      - name: Publish to npm
        run: npm publish --tag "${{ needs.check-changes.outputs.npmTag }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: echo "Published @terreno/admin-spa@${{ needs.check-changes.outputs.version }}" >> $GITHUB_STEP_SUMMARY

  publish-ai:
    needs: [check-changes, publish-api, publish-test]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: ai
    steps:
      - uses: actions/checkout@v6.0.3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Use Node.js 22
        uses: actions/setup-node@v7
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Validate required secrets
        run: |
          missing=()
          if [ -z "${{ secrets.NPM_TOKEN }}" ]; then missing+=("NPM_TOKEN"); fi
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required secrets: ${missing[*]}"
            exit 1
          fi

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ github.ref }}-${{ hashFiles('bun.lock', 'package.json') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ github.ref }}-
            bun-${{ runner.os }}-

      - name: Start MongoDB
        uses: supercharge/mongodb-github-action@1.12.1
        with:
          mongodb-version: 6.0
          mongodb-image: mirror.gcr.io/library/mongo

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Update version
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); pkg.version = '$VERSION'; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');"

      - name: Replace catalog and workspace references
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "
          const fs = require('fs');
          const rootPkg = JSON.parse(fs.readFileSync('../package.json', 'utf8'));
          const catalog = rootPkg.catalog || {};
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          const version = process.env.VERSION;
          for (const depType of ['dependencies', 'devDependencies', 'peerDependencies']) {
            if (!pkg[depType]) continue;
            for (const [key, value] of Object.entries(pkg[depType])) {
              if (value === 'catalog:') {
                if (!catalog[key]) throw new Error(\`Catalog entry not found for \${key}\`);
                pkg[depType][key] = catalog[key];
              } else if (value === 'workspace:*' && key.startsWith('@terreno/')) {
                pkg[depType][key] = version;
              }
            }
          }
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "
        env:
          VERSION: ${{ needs.check-changes.outputs.version }}

      - name: Install dependencies after replacement
        run: bun install

      - name: Compile workspace dependencies
        run: node ../.github/scripts/compile-workspace-deps.js

      - name: Compile
        run: bun run compile

      - name: Run tests
        run: bun run test

      - name: Publish to npm
        run: npm publish --tag "${{ needs.check-changes.outputs.npmTag }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: echo "Published @terreno/ai@${{ needs.check-changes.outputs.version }}" >> $GITHUB_STEP_SUMMARY

  publish-api-health:
    needs: [check-changes, publish-api, publish-test]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: api-health
    steps:
      - uses: actions/checkout@v6.0.3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Use Node.js 22
        uses: actions/setup-node@v7
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Validate required secrets
        run: |
          missing=()
          if [ -z "${{ secrets.NPM_TOKEN }}" ]; then missing+=("NPM_TOKEN"); fi
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required secrets: ${missing[*]}"
            exit 1
          fi

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ github.ref }}-${{ hashFiles('bun.lock', 'package.json') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ github.ref }}-
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Update version
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); pkg.version = '$VERSION'; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');"

      - name: Replace catalog and workspace references
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "
          const fs = require('fs');
          const rootPkg = JSON.parse(fs.readFileSync('../package.json', 'utf8'));
          const catalog = rootPkg.catalog || {};
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          const version = process.env.VERSION;
          for (const depType of ['dependencies', 'devDependencies', 'peerDependencies']) {
            if (!pkg[depType]) continue;
            for (const [key, value] of Object.entries(pkg[depType])) {
              if (value === 'catalog:') {
                if (!catalog[key]) throw new Error(\`Catalog entry not found for \${key}\`);
                pkg[depType][key] = catalog[key];
              } else if (value === 'workspace:*' && key.startsWith('@terreno/')) {
                pkg[depType][key] = version;
              }
            }
          }
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "
        env:
          VERSION: ${{ needs.check-changes.outputs.version }}

      - name: Install dependencies after replacement
        run: bun install

      - name: Compile workspace dependencies
        run: node ../.github/scripts/compile-workspace-deps.js

      - name: Compile
        run: bun run compile

      - name: Run tests
        run: bun run test

      - name: Publish to npm
        run: npm publish --tag "${{ needs.check-changes.outputs.npmTag }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: echo "Published @terreno/api-health@${{ needs.check-changes.outputs.version }}" >> $GITHUB_STEP_SUMMARY

  publish-comms:
    needs: [check-changes, publish-api, publish-test]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: comms
    steps:
      - uses: actions/checkout@v6.0.3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Use Node.js 22
        uses: actions/setup-node@v7
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Validate required secrets
        run: |
          missing=()
          if [ -z "${{ secrets.NPM_TOKEN }}" ]; then missing+=("NPM_TOKEN"); fi
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required secrets: ${missing[*]}"
            exit 1
          fi

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ github.ref }}-${{ hashFiles('bun.lock', 'package.json') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ github.ref }}-
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Update version
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); pkg.version = '$VERSION'; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');"

      - name: Replace catalog and workspace references
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "
          const fs = require('fs');
          const rootPkg = JSON.parse(fs.readFileSync('../package.json', 'utf8'));
          const catalog = rootPkg.catalog || {};
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          const version = process.env.VERSION;
          for (const depType of ['dependencies', 'devDependencies', 'peerDependencies']) {
            if (!pkg[depType]) continue;
            for (const [key, value] of Object.entries(pkg[depType])) {
              if (value === 'catalog:') {
                if (!catalog[key]) throw new Error(\`Catalog entry not found for \${key}\`);
                pkg[depType][key] = catalog[key];
              } else if (value === 'workspace:*' && key.startsWith('@terreno/')) {
                pkg[depType][key] = version;
              }
            }
          }
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "
        env:
          VERSION: ${{ needs.check-changes.outputs.version }}

      - name: Install dependencies after replacement
        run: bun install

      - name: Compile workspace dependencies
        run: node ../.github/scripts/compile-workspace-deps.js

      - name: Compile
        run: bun run compile

      - name: Run tests
        run: bun run test

      - name: Publish to npm
        run: npm publish --tag "${{ needs.check-changes.outputs.npmTag }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: echo "Published @terreno/comms@${{ needs.check-changes.outputs.version }}" >> $GITHUB_STEP_SUMMARY

  publish-feature-flags:
    needs: [check-changes, publish-api, publish-test]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: feature-flags
    steps:
      - uses: actions/checkout@v6.0.3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Use Node.js 22
        uses: actions/setup-node@v7
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Validate required secrets
        run: |
          missing=()
          if [ -z "${{ secrets.NPM_TOKEN }}" ]; then missing+=("NPM_TOKEN"); fi
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required secrets: ${missing[*]}"
            exit 1
          fi

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ github.ref }}-${{ hashFiles('bun.lock', 'package.json') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ github.ref }}-
            bun-${{ runner.os }}-

      - name: Start MongoDB
        uses: supercharge/mongodb-github-action@1.12.1
        with:
          mongodb-version: 6.0
          mongodb-image: mirror.gcr.io/library/mongo

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Update version
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); pkg.version = '$VERSION'; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');"

      - name: Replace catalog and workspace references
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "
          const fs = require('fs');
          const rootPkg = JSON.parse(fs.readFileSync('../package.json', 'utf8'));
          const catalog = rootPkg.catalog || {};
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          const version = process.env.VERSION;
          for (const depType of ['dependencies', 'devDependencies', 'peerDependencies']) {
            if (!pkg[depType]) continue;
            for (const [key, value] of Object.entries(pkg[depType])) {
              if (value === 'catalog:') {
                if (!catalog[key]) throw new Error(\`Catalog entry not found for \${key}\`);
                pkg[depType][key] = catalog[key];
              } else if (value === 'workspace:*' && key.startsWith('@terreno/')) {
                pkg[depType][key] = version;
              }
            }
          }
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "
        env:
          VERSION: ${{ needs.check-changes.outputs.version }}

      - name: Install dependencies after replacement
        run: bun install

      - name: Compile workspace dependencies
        run: node ../.github/scripts/compile-workspace-deps.js

      - name: Compile
        run: bun run compile

      - name: Run tests
        run: bun run test

      - name: Publish to npm
        run: npm publish --tag "${{ needs.check-changes.outputs.npmTag }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: echo "Published @terreno/feature-flags@${{ needs.check-changes.outputs.version }}" >> $GITHUB_STEP_SUMMARY

  publish-mcp:
    needs: [check-changes, publish-api, publish-test]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: mcp-server
    steps:
      - uses: actions/checkout@v6.0.3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Use Node.js 22
        uses: actions/setup-node@v7
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Validate required secrets
        run: |
          missing=()
          if [ -z "${{ secrets.NPM_TOKEN }}" ]; then missing+=("NPM_TOKEN"); fi
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required secrets: ${missing[*]}"
            exit 1
          fi

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ github.ref }}-${{ hashFiles('bun.lock', 'package.json') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ github.ref }}-
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Update version
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); pkg.version = '$VERSION'; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');"

      - name: Replace catalog and workspace references
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "
          const fs = require('fs');
          const rootPkg = JSON.parse(fs.readFileSync('../package.json', 'utf8'));
          const catalog = rootPkg.catalog || {};
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          const version = process.env.VERSION;
          for (const depType of ['dependencies', 'devDependencies', 'peerDependencies']) {
            if (!pkg[depType]) continue;
            for (const [key, value] of Object.entries(pkg[depType])) {
              if (value === 'catalog:') {
                if (!catalog[key]) throw new Error(\`Catalog entry not found for \${key}\`);
                pkg[depType][key] = catalog[key];
              } else if (value === 'workspace:*' && key.startsWith('@terreno/')) {
                pkg[depType][key] = version;
              }
            }
          }
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "
        env:
          VERSION: ${{ needs.check-changes.outputs.version }}

      - name: Install dependencies after replacement
        run: bun install

      - name: Compile workspace dependencies
        run: node ../.github/scripts/compile-workspace-deps.js

      - name: Compile
        run: bun run compile

      - name: Run tests
        run: bun run test

      - name: Publish to npm
        run: npm publish --tag "${{ needs.check-changes.outputs.npmTag }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: echo "Published @terreno/mcp@${{ needs.check-changes.outputs.version }}" >> $GITHUB_STEP_SUMMARY

  publish-syncdb:
    needs: [check-changes]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: syncdb
    steps:
      - uses: actions/checkout@v6.0.3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Use Node.js 22
        uses: actions/setup-node@v7
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Validate required secrets
        run: |
          missing=()
          if [ -z "${{ secrets.NPM_TOKEN }}" ]; then missing+=("NPM_TOKEN"); fi
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required secrets: ${missing[*]}"
            exit 1
          fi

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ github.ref }}-${{ hashFiles('bun.lock', 'package.json') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ github.ref }}-
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Update version
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); pkg.version = '$VERSION'; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');"

      - name: Replace catalog and workspace references
        run: |
          node -e "
          const fs = require('fs');
          const rootPkg = JSON.parse(fs.readFileSync('../package.json', 'utf8'));
          const catalog = rootPkg.catalog || {};
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          const version = process.env.VERSION;
          for (const depType of ['dependencies', 'devDependencies', 'peerDependencies']) {
            if (!pkg[depType]) continue;
            for (const [key, value] of Object.entries(pkg[depType])) {
              if (value === 'catalog:') {
                if (!catalog[key]) throw new Error(\`Catalog entry not found for \${key}\`);
                pkg[depType][key] = catalog[key];
              } else if (value === 'workspace:*' && key.startsWith('@terreno/')) {
                pkg[depType][key] = version;
              }
            }
          }
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "
        env:
          VERSION: ${{ needs.check-changes.outputs.version }}

      - name: Install dependencies after replacement
        run: bun install

      - name: Compile workspace dependencies
        run: node ../.github/scripts/compile-workspace-deps.js

      - name: Compile
        run: bun run compile

      - name: Run tests
        run: bun run test

      - name: Publish to npm
        run: npm publish --tag "${{ needs.check-changes.outputs.npmTag }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: echo "Published @terreno/syncdb@${{ needs.check-changes.outputs.version }}" >> $GITHUB_STEP_SUMMARY

  update-version-on-master:
    needs: [check-changes, publish-api, publish-test, publish-ui, publish-rtk, publish-admin-backend, publish-admin-frontend, publish-admin-spa, publish-ai, publish-api-health, publish-comms, publish-feature-flags, publish-mcp, publish-syncdb]
    # Run if any package was published and this is not a prerelease tag
    if: |
      always() &&
      needs.check-changes.outputs.isPrerelease != 'true' &&
      (needs.publish-api.result == 'success' || needs.publish-test.result == 'success' || needs.publish-ui.result == 'success' || needs.publish-rtk.result == 'success' || needs.publish-admin-backend.result == 'success' || needs.publish-admin-frontend.result == 'success' || needs.publish-admin-spa.result == 'success' || needs.publish-ai.result == 'success' || needs.publish-api-health.result == 'success' || needs.publish-comms.result == 'success' || needs.publish-feature-flags.result == 'success' || needs.publish-mcp.result == 'success' || needs.publish-syncdb.result == 'success')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Validate required secrets
        env:
          REPO_ADMIN_TOKEN: ${{ secrets.REPO_ADMIN_TOKEN }}
        run: |
          missing=()
          if [ -z "$REPO_ADMIN_TOKEN" ]; then missing+=("REPO_ADMIN_TOKEN"); fi
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required secrets: ${missing[*]}"
            exit 1
          fi

      - uses: actions/checkout@v6.0.3
        with:
          ref: master
          token: ${{ secrets.REPO_ADMIN_TOKEN }}
          persist-credentials: true

      - name: Setup Git
        run: |
          git config --global user.name "joshgachnang"
          git config --global user.email "josh@flourish.health"

      - name: Update package versions
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          update_version() {
            local dir=$1
            local result=$2
            # A package can be published from a release branch before it exists on
            # master, so a missing directory here is expected, not an error.
            if [ ! -d "$dir" ]; then
              echo "Skipping $dir: not present on master"
              return 0
            fi
            if [ "$result" == "success" ]; then
              cd "$dir" && node -e "const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); pkg.version = '$VERSION'; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');" && cd ..
            fi
          }

          update_version api "${{ needs.publish-api.result }}"
          update_version test "${{ needs.publish-test.result }}"
          update_version ui "${{ needs.publish-ui.result }}"
          update_version rtk "${{ needs.publish-rtk.result }}"
          update_version admin-backend "${{ needs.publish-admin-backend.result }}"
          update_version admin-frontend "${{ needs.publish-admin-frontend.result }}"
          update_version admin-spa "${{ needs.publish-admin-spa.result }}"
          update_version ai "${{ needs.publish-ai.result }}"
          update_version api-health "${{ needs.publish-api-health.result }}"
          update_version comms "${{ needs.publish-comms.result }}"
          update_version feature-flags "${{ needs.publish-feature-flags.result }}"
          update_version mcp-server "${{ needs.publish-mcp.result }}"
          update_version syncdb "${{ needs.publish-syncdb.result }}"

      - name: Commit and push to master
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          git add -A
          if git diff --cached --quiet; then
            echo "No version changes to commit"
          else
            git commit -m "chore: bump package versions to ${VERSION}"
            git pull --rebase origin master
            git push origin master
          fi

  trigger-demo-deploy:
    name: Trigger demo deploy
    needs: update-version-on-master
    # `always()` avoids the skip propagating from earlier publish-* jobs — see the same
    # note in demo-deploy.yml. The version bump commit doesn't reliably re-trigger
    # demo-deploy.yml's path-filtered push trigger (tag pushes carry no file diff, and a
    # skipped/failed package doesn't touch ui/package.json), so force a redeploy here to
    # guarantee the public demo always matches the latest release.
    if: always() && needs.update-version-on-master.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - name: Dispatch demo-deploy workflow
        uses: actions/github-script@v9.0.0
        # Dispatch against the release tag itself (not master), so the demo is pinned to
        # exactly what was published rather than whatever master happens to be at the
        # moment this runs (which may include later, unreleased commits). This requires
        # demo-deploy.yml to already have the workflow_dispatch trigger at the tagged
        # commit, which holds for any tag cut from master after this change lands.
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "demo-deploy.yml",
              ref: process.env.RELEASE_TAG,
            });

  cut-docs-version:
    needs: [check-changes, update-version-on-master]
    if: |
      needs.update-version-on-master.result == 'success' &&
      needs.check-changes.outputs.isPrerelease != 'true' &&
      endsWith(needs.check-changes.outputs.version, '.0')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Validate required secrets
        env:
          REPO_ADMIN_TOKEN: ${{ secrets.REPO_ADMIN_TOKEN }}
        run: |
          missing=()
          if [ -z "$REPO_ADMIN_TOKEN" ]; then missing+=("REPO_ADMIN_TOKEN"); fi
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required secrets: ${missing[*]}"
            exit 1
          fi

      - uses: actions/checkout@v6.0.3
        with:
          ref: master
          token: ${{ secrets.REPO_ADMIN_TOKEN }}
          persist-credentials: true

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Setup Git
        run: |
          git config --global user.name "joshgachnang"
          git config --global user.email "josh@flourish.health"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate docs inputs
        run: |
          bun run compile
          bun run types
        working-directory: ui

      - name: Cut docs version
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          cd website
          bun run generate
          bun run docs:version "$VERSION"
          bun run prune-versions

      - name: Commit versioned docs
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          git add website/versioned_docs website/versioned_sidebars website/versions.json
          if git diff --cached --quiet; then
            echo "No docs version changes to commit"
          else
            git commit -m "chore: cut docs version ${VERSION}"
            git pull --rebase origin master
            git push origin master
          fi

  notify:
    needs: [check-changes, publish-api, publish-test, publish-ui, publish-rtk, publish-admin-backend, publish-admin-frontend, publish-admin-spa, publish-ai, publish-api-health, publish-comms, publish-feature-flags, publish-mcp, publish-syncdb]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Validate required secrets
        env:
          ZOOM_WEBHOOK_TOKEN: ${{ secrets.ZOOM_WEBHOOK_TOKEN }}
          ZOOM_WEBHOOK_URL: ${{ secrets.ZOOM_WEBHOOK_URL }}
        run: |
          missing=()
          if [ -z "$ZOOM_WEBHOOK_TOKEN" ]; then missing+=("ZOOM_WEBHOOK_TOKEN"); fi
          if [ -z "$ZOOM_WEBHOOK_URL" ]; then missing+=("ZOOM_WEBHOOK_URL"); fi
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required secrets: ${missing[*]}"
            exit 1
          fi

      - name: Build notification message
        id: message
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          MESSAGE="Release $VERSION:\n"

          add_status() {
            local name=$1
            local result=$2
            if [ "$result" == "success" ]; then
              MESSAGE="$MESSAGE✅ ${name}@$VERSION published\n"
            elif [ "$result" == "skipped" ]; then
              MESSAGE="$MESSAGE⏭️ ${name} skipped\n"
            elif [ "$result" == "failure" ]; then
              MESSAGE="$MESSAGE❌ ${name} failed\n"
            fi
          }

          add_status "@terreno/api" "${{ needs.publish-api.result }}"
          add_status "@terreno/test" "${{ needs.publish-test.result }}"
          add_status "@terreno/ui" "${{ needs.publish-ui.result }}"
          add_status "@terreno/rtk" "${{ needs.publish-rtk.result }}"
          add_status "@terreno/admin-backend" "${{ needs.publish-admin-backend.result }}"
          add_status "@terreno/admin-frontend" "${{ needs.publish-admin-frontend.result }}"
          add_status "@terreno/admin-spa" "${{ needs.publish-admin-spa.result }}"
          add_status "@terreno/ai" "${{ needs.publish-ai.result }}"
          add_status "@terreno/api-health" "${{ needs.publish-api-health.result }}"
          add_status "@terreno/comms" "${{ needs.publish-comms.result }}"
          add_status "@terreno/feature-flags" "${{ needs.publish-feature-flags.result }}"
          add_status "@terreno/mcp" "${{ needs.publish-mcp.result }}"
          add_status "@terreno/syncdb" "${{ needs.publish-syncdb.result }}"
          echo "message=$MESSAGE" >> $GITHUB_OUTPUT

      - name: Zoom Chat Notification
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          TAG_NAME="${{ github.ref_name }}"
          REPO="${{ github.repository }}"
          TAG_URL="https://github.com/${REPO}/releases/tag/${TAG_NAME}"
          COMMIT_SHA="${{ github.sha }}"
          COMMIT_SHORT="${COMMIT_SHA:0:7}"
          
          MESSAGE="${{ steps.message.outputs.message }}"
          MESSAGE="${MESSAGE}Tag: ${TAG_NAME}\n"
          MESSAGE="${MESSAGE}Commit: ${COMMIT_SHORT}\n"
          MESSAGE="${MESSAGE}View: ${TAG_URL}"
          
          # Convert \n escape sequences to actual newlines
          MESSAGE=$(printf "%b" "$MESSAGE")
          
          curl -X POST "${{ secrets.ZOOM_WEBHOOK_URL }}" \
            -H "Authorization: ${{ secrets.ZOOM_WEBHOOK_TOKEN }}" \
            -H "Content-Type: text/plain" \
            --data-raw "$MESSAGE"
