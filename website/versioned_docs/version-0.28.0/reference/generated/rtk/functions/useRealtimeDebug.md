> **useRealtimeDebug**(`baseUrl`, `refreshKey?`): `boolean`

Syncs websocket debug logging with the backend RealtimeApp debug flag.
Also respects WEBSOCKETS_DEBUG from app config via isWebsocketsDebugEnabled().

## Parameters

### baseUrl

`string`

### refreshKey?

`unknown`

## Returns

`boolean`
