> **getQuerySubscriptionsForCollection**(`collection`): `object`[]

Get all unique query subscriptions for a given collection.
Used by the change stream watcher to evaluate which query rooms to emit to.

## Parameters

### collection

`string`

## Returns

`object`[]
