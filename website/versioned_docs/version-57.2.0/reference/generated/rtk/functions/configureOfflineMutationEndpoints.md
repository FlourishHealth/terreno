> **configureOfflineMutationEndpoints**(`endpoints`): `void`

Register mutation endpoints that should be queued instead of sent while offline.

## Parameters

### endpoints

`string`[]

## Returns

`void`
