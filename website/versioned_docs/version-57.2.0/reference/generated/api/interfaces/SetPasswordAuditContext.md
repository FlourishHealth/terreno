Optional audit context for [setPasswordForUser](../functions/setPasswordForUser.md) — never includes the password itself.

## Properties

### adminId?

> `optional` **adminId?**: `unknown`

The admin performing the change, when set via an admin-only route.
