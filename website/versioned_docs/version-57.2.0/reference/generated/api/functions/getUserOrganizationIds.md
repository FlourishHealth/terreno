> **getUserOrganizationIds**(`user?`): `string`[]

Reads the caller's organization ids, tolerating users without the `organizationIds` field.

## Parameters

### user?

[`User`](../interfaces/User.md)

## Returns

`string`[]
