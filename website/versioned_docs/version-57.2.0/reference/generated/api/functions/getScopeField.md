> **getScopeField**(`scope`): `string` \| `null`

The document field a scope reads, or null for broadcast/custom scopes.

## Parameters

### scope

[`SyncScope`](../type-aliases/SyncScope.md)

## Returns

`string` \| `null`
