---
category: Added
---

`@terreno/ui` now ships owned-SVG `LineChart`, `BarChart`, `AreaChart`, and `DonutChart`
plus an eager `DashboardGrid` for wrapping caller `Card`s. Charts use `react-native-svg`
and private d3 helpers — not `victory-native`.
