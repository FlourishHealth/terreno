---
category: Added
---

`terreno_search_docs` and `terreno_get_component_docs` accept an optional
`version` so agents can search retained docs snapshots for the consumer's
`@terreno/*` lockstep version.
