---
category: Added
---

`SyncConfig.adminBroadcast` (default `false`) is stored on collection registration so hosts can opt a synced model into `{collection}|admin` fan-in without a second `registerSync`.
