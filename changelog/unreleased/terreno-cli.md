---
category: Added
---

Add `@terreno/cli` (`terreno`) for docs search, codegen (syncdb, RTK SDK, models, routes, screens, forms, admin), bootstrap, local logs/db/info, OpenAPI `api list|call|request`, and `generate rest-cli` to scaffold an app CLI from a backend spec.
