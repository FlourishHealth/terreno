---
category: Changed
---

Unreleased notes are one file per feature in `changelog/unreleased/` instead of a shared
`CHANGELOG.md` `## [Unreleased]` section, so parallel PRs no longer conflict on that file.
