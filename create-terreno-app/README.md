# create-terreno-app

Generates a full-stack Terreno application scaffold (backend, frontend, CI, MCP config).

CLI wiring lands in a later task; this package currently exports `generateAllFiles` for MCP and future `bunx create-terreno-app`.
